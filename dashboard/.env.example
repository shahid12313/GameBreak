# The backend API's base URL, including /api. During local development this
# points at your locally-running backend; in production it points at your
# deployed Render URL (see the root README, Part 5).
VITE_API_URL=http://localhost:5000/api
