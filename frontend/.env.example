# The backend API's base URL, including /api.
VITE_API_URL=http://localhost:5000/api

# Where the "Admin" button in the top navigation sends staff to sign in.
# This is a full URL because the dashboard is a completely separate
# application/deployment — see the root README, Part 4.
VITE_DASHBOARD_URL=http://localhost:5174
